{
  "name": "pericia-forense",
  "version": "2.0.0",
  "main": "src/server.js",
  "scripts": {
    "test": "cross-env NODE_ENV=test jest --runInBand --coverage"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  }
}